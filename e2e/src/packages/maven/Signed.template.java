package io.repsy.e2e;

/** The one class of the signed-deploy fixture, so the jar and the sources jar are real. */
public final class Signed {
  public static final String MARKER = "{{{marker}}}";

  private Signed() {}
}

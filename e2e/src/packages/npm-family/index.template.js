// Repsy e2e test package (clients/npm-family). Static and dependency-free on purpose: this harness
// must never let a package manager reach the real npmjs.org for anything.
module.exports = { name: 'repsy-e2e' };
